SQL is a pretty straight forward language. 

In this unit the `people` database was already created, let's learn how to build a similar database together with its tables from the ground up in the next unit.
